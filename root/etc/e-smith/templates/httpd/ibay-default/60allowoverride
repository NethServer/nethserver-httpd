#
# 60allowoverride is { $AllowOverrideStatus }
#
{
    if(($AllowOverrideStatus) eq 'enabled') {
       $OUT = <<OVERRIDE;
AllowOverride All
<Files .htwritable>
    Order deny,allow
    Deny from all
</Files>
OVERRIDE
    } else {
      $OUT = '    AllowOverride None'
    }
}


