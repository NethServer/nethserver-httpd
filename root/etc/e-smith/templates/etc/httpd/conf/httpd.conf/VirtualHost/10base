#
# 10base
#
ServerName {$ServerName}
DocumentRoot {$DocumentRoot || $defaultDocumentRoot || '/var/www/html' }
SSLEngine {
    if(defined($SslMode) && $Port != 80 &&
       ($SslMode eq 'required' || $SslMode eq 'optional')) {
	'On';
    } else {
	'Off';
    }
}


